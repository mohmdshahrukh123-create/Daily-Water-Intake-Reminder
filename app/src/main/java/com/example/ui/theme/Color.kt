package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Hydration Blue & White Premium Theme Colors
val OceanBlue = Color(0xFF0F62FE)      // Accent Brand Blue (M3 primary)
val IceBlue = Color(0xFF6EA8FF)        // Light Accent Blue (M3 secondary)
val CrispWhite = Color(0xFFFFFFFF)     // Clean white surface elements
val FrostBlue = Color(0xFFE3EDFD)      // Light card background / tint
val DeepMidnight = Color(0xFF070F2B)   // Midnight theme base (when Sun sets)
val StarrySpace = Color(0xFF1B1A55)    // Cosmic Purple-Blue blend for bedtime card

// Semantic Schemes
val LightBluePrimary = Color(0xFF0F62FE)
val LightBlueSecondary = Color(0xFF3F8CFF)
val LightBlueBackground = Color(0xFFF3F7FD) // Pure water freshness bg
val LightBlueSurface = Color(0x99FFFFFF)    // Glassmorphic translucent surface

val DarkBluePrimary = Color(0xFF7CAFFF)
val DarkBlueSecondary = Color(0xFF3F8CFF)
val DarkBlueBackground = Color(0xFF040A1E)  // Dark deep ocean
val DarkBlueSurface = Color(0x1AFFFFFF)     // Glassmorphism translucent dark card
